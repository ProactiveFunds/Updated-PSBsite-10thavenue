---
category: Sections
---
The partner and verifier logo wall.

Renders each mark in a quiet, evenly-spaced row. Logos are theme-aware: the
colour marks show in light theme and the white marks in dark, via the
`.logo-light` / `.logo-dark` pair in `responsive.css`.

```jsx
<Partners />
```
